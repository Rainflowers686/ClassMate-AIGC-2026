# Stage 8A-3 真实 Bridge 完成后后续 Issue 草案

> 30 个后续 issue，供明天 Claude 完成真实 reflection bridge 后逐步消化。
> 当前阶段不实现，仅记录规划。每个 issue 标注目标、文件范围、验收标准、非目标、风险。

---

### Issue 01: 端侧模型真机性能基准记录

- **目标**：在云真机 X300 Pro 上记录端侧 BlueLM 3B 的性能基准数据
- **文件范围**：`docs/testing/stage8a3_real_device_test_sheet.md`（填写结果）
- **验收标准**：
  - 记录 init 耗时（秒）
  - 记录首个 token 耗时（毫秒）
  - 记录不同长度 prompt 的生成速度
  - 记录内存峰值
- **非目标**：不做性能优化
- **风险**：云真机性能可能与用户设备差异大

---

### Issue 02: 模型路径配置化

- **目标**：将端侧模型路径从硬编码改为运行时配置
- **文件范围**：`core/src/.../model/`, `config.local.json`（字段新增，不读内容）
- **验收标准**：
  - 支持通过配置指定模型目录
  - 默认路径 `/sdcard/1225/`（官方约定）
  - 路径不存在时给出明确错误提示
- **非目标**：不做模型的自动下载功能
- **风险**：路径配置错误导致 init 失败

---

### Issue 03: 端侧 Ask 接入

- **目标**：将端侧模型接入 Ask This Lesson 的简单问答场景
- **文件范围**：`core/src/.../ask/`, `core/src/.../provider/`
- **验收标准**：
  - Ask 请求优先走端侧 Provider
  - 端侧不可用时自动 fallback 到云端
  - 不改变 Ask 的输出格式契约
- **非目标**：不接入复杂的多步推理 Ask
- **风险**：端侧回答质量可能不满足 Ask 场景的准确率要求

---

### Issue 04: 端侧 StudyReport 接入

- **目标**：将端侧模型接入学习报告的场景化摘要
- **文件范围**：`core/src/.../report/`
- **验收标准**：
  - 简单摘要类报告尝试走端侧
  - 复杂报告自动路由到云端
  - 端侧生成的结果标注来源
- **非目标**：不做端侧的长文本报告生成
- **风险**：摘要质量可能明显低于云端

---

### Issue 05: 端侧 Practice 接入

- **目标**：将端侧模型接入自适应练习的简单题目生成
- **文件范围**：`core/src/.../practice/`
- **验收标准**：
  - 选择题/填空题走端侧
  - 主观题走云端
  - 端侧题目标注来源
- **非目标**：不做端侧的实时评分
- **风险**：题目质量参差不齐

---

### Issue 06: 多模态图片题目诊断

- **目标**：端侧 VIT + 云端模型联合处理图片题目
- **文件范围**：`core/src/.../image/`, `core/src/.../provider/`
- **验收标准**：
  - 端侧 VIT 编码图片
  - 编码结果传给模型（端侧或云端）进行题目分析
  - 不保存原始图片
- **非目标**：不做端侧完整题目解答
- **风险**：VIT 编码精度影响分析质量

---

### Issue 07: 课件截图理解

- **目标**：对 PPT/PDF 课件截图进行端侧预处理
- **文件范围**：`core/src/.../material/`, `core/src/.../image/`
- **验收标准**：
  - 端侧 VIT 对课件截图进行编码
  - 云端模型基于编码进行内容理解
  - 不保存原始截图
- **非目标**：不做端侧完整 OCR
- **风险**：课件截图中的文字密度较高，VIT 效果待验证

---

### Issue 08: 板书照片理解

- **目标**：对黑板/白板照片进行端侧预处理
- **文件范围**：`core/src/.../material/`, `core/src/.../image/`
- **验收标准**：
  - 端侧 VIT 编码板书照片
  - 云端模型识别板书内容
  - 不保存原始照片
- **非目标**：不做实时板书识别
- **风险**：手写板书识别难度远高于印刷体

---

### Issue 09: ImageUnderstandingSource 接口定义

- **目标**：定义统一的图像理解来源抽象接口
- **文件范围**：`core/src/.../image/ImageUnderstandingSource.kt`
- **验收标准**：
  - 接口支持端侧 VIT 和云端视觉两种来源
  - 支持 Bitmap 输入
  - 返回结构化理解结果
- **非目标**：不做图像理解的 UI 组件
- **风险**：接口设计需同时满足端侧和云端两种模式

---

### Issue 10: MaterialBundle 图像来源扩展

- **目标**：在 MaterialBundle 中增加图像来源类型
- **文件范围**：`core/src/.../material/MaterialBundle.kt`
- **验收标准**：
  - 新增 ImageSource 枚举值
  - 支持课件截图、板书照片、题目截图三类
  - 与现有文本来源不冲突
- **非目标**：不做图像的存储管理
- **风险**：MaterialBundle 可能变得过于复杂

---

### Issue 11: Export 报告附图像解释

- **目标**：在导出的学习报告中附带图像理解结果
- **文件范围**：`core/src/.../export/`
- **验收标准**：
  - 报告包含图像来源标注
  - 图像理解摘要嵌入报告
  - 不导出原始图片
- **非目标**：不做报告中嵌入图片
- **风险**：报告体积增大

---

### Issue 12: 本地文本审核 SDK

- **目标**：评估并接入本地文本审核能力
- **文件范围**：`core/src/.../safety/`
- **验收标准**：
  - 在端侧输出后执行本地文本审核
  - 过滤不宜内容
  - 审核结果可追溯
- **非目标**：不做实时对话审核
- **风险**：本地审核 SDK 可能增加 APK 体积

---

### Issue 13: 端侧输出 Redaction

- **目标**：对端侧模型输出进行敏感信息脱敏
- **文件范围**：`core/src/.../safety/`
- **验收标准**：
  - 检测并脱敏手机号、邮箱、身份证号等 PII
  - 检测并脱敏可能的密钥/令牌
  - 脱敏后的文本保留语义
- **非目标**：不做云端级别的深度内容审核
- **风险**：脱敏规则可能误伤正常内容

---

### Issue 14: 权限最小化审计

- **目标**：审计端侧功能所需权限，确保权限最小化
- **文件范围**：`app/src/main/AndroidManifest.xml`
- **验收标准**：
  - 端侧不需要的权限全部移除
  - 存储权限仅用于模型路径
  - 网络权限仅用于模型下载
- **非目标**：不改动其他功能的权限
- **风险**：移除权限可能影响其他功能

---

### Issue 15: 错误码完整映射表

- **目标**：建立端侧 SDK 错误码到用户文案的完整映射
- **文件范围**：`docs/product/stage8a3_ondevice_error_copy.md`（补充）
- **验收标准**：
  - 覆盖所有已知 SDK 错误码
  - 每个错误码有用户文案和调试标签
  - 标注严重级别
- **非目标**：不做错误恢复自动化
- **风险**：SDK 错误码文档可能不完整

---

### Issue 16: Fallback UI 优化

- **目标**：优化 fallback 状态下的 UI 提示
- **文件范围**：`app/src/.../ui/settings/`, `app/src/.../ui/chat/`
- **验收标准**：
  - 当前 Provider 路径在对话界面可见
  - fallback 发生时有非侵入式提示
  - 用户可手动切换 Provider
- **非目标**：不做复杂的 Provider 切换动画
- **风险**：UI 改动可能影响现有布局

---

### Issue 17: 端侧耗时指标埋点

- **目标**：记录端侧推理的耗时指标
- **文件范围**：`core/src/.../metrics/`
- **验收标准**：
  - 记录 init 耗时
  - 记录每次 generate 的 total/首 token 耗时
  - 记录 callVit 耗时
  - 指标仅在本地，不上传
- **非目标**：不做实时性能面板
- **风险**：埋点代码可能引入性能开销

---

### Issue 18: 发热风险与 Throttling 说明

- **目标**：编写端侧模型的发热风险用户说明
- **文件范围**：`docs/product/stage8a3_ondevice_error_copy.md`（补充）
- **验收标准**：
  - 说明长时间端侧推理可能导致设备发热
  - 说明系统 throttling 会降低推理速度
  - 建议单次推理不超过 X 秒
- **非目标**：不做温度监控功能
- **风险**：发热说明可能影响用户对端侧功能的信任

---

### Issue 19: Release 生命周期完整测试

- **目标**：验证 release() 在各种场景下的正确性
- **文件范围**：测试代码（不影响生产代码）
- **验收标准**：
  - release 后 generate 不可用（或自动 re-init）
  - 页面退出时正确 release
  - Activity 销毁时正确 release
  - 重复 release 不崩溃
- **非目标**：不做生产代码改动
- **风险**：内存泄漏可能导致长时间使用后崩溃

---

### Issue 20: 内存泄漏专项检查

- **目标**：使用 LeakCanary 或 Android Profiler 检查端侧相关内存泄漏
- **文件范围**：不修改文件，仅检查
- **验收标准**：
  - TokenCallback 在 release 后被正确回收
  - Bitmap 在 callVit 后被正确回收
  - LlmManager 实例在 release 后不再持有引用
- **非目标**：不做内存优化（仅发现问题）
- **风险**：Native 层内存泄漏更难检测

---

### Issue 21: APK 体积影响说明

- **目标**：量化端侧 SDK 对 APK 体积的影响
- **文件范围**：`docs/architecture/`（新增文档）
- **验收标准**：
  - 记录 AAR 本身的体积
  - 记录 .so 文件的体积（按 ABI 分列）
  - 记录模型文件体积（不作为 APK 一部分）
  - 对比接入前后的 APK 体积
- **非目标**：不做体积优化
- **风险**：评委可能关注 APK 体积膨胀

---

### Issue 22: 评委 Proof 截图拍摄计划

- **目标**：按照 proof pack checklist 完成所有截图的拍摄
- **文件范围**：`docs/competition/stage8a3_ondevice_proof_pack_checklist.md`
- **验收标准**：
  - 17 张截图全部完成
  - 每张截图验证无敏感信息
  - 截图放入 proof_out 目录
- **非目标**：不做录屏
- **风险**：部分截图依赖真机环境，可能无法获取

---

### Issue 23: 云真机 Smoke 自动化

- **目标**：编写云真机 smoke 测试的一键执行脚本
- **文件范围**：`scripts/qa/`（新增脚本）
- **验收标准**：
  - 自动检查 SDK_PRESENT
  - 自动检查 init 返回码
  - 自动检查 forbid tracked files
  - 生成 smoke 报告
- **非目标**：不做真机上自动化 UI 测试
- **风险**：云真机环境每次都不同

---

### Issue 24: 决赛系统级嵌入预留

- **目标**：评估端侧模型在决赛中的系统级集成方案
- **文件范围**：`docs/architecture/`（新增文档）
- **验收标准**：
  - 评估 system-level API 的可行性
  - 评估 NPU 直接调用的可行性
  - 评估与其他系统服务的协作方式
- **非目标**：不做任何系统级代码
- **风险**：系统级 API 可能需要厂商特殊授权

---

### Issue 25: 端侧多模型 Provider 扩展接口

- **目标**：设计支持未来切换端侧模型的抽象接口
- **文件范围**：`core/src/.../provider/`（接口定义，不实现）
- **验收标准**：
  - Provider 接口支持不同端侧模型
  - 模型切换不影响上层业务
  - 配置化模型选择
- **非目标**：不做多模型的实际实现
- **风险**：抽象过度可能导致接口复杂

---

### Issue 26: 端侧对话历史管理

- **目标**：设计端侧对话的上下文窗口管理策略
- **文件范围**：设计文档，不涉及代码
- **验收标准**：
  - 定义端侧模型的上下文窗口大小（nCtx）
  - 定义超出窗口时的截断策略
  - 定义多轮对话的内存管理
- **非目标**：不做对话历史的持久化
- **风险**：上下文截断可能导致回答质量下降

---

### Issue 27: 端侧与云端模型输出一致性对比

- **目标**：设计评估框架，对比端侧和云端输出的一致性
- **文件范围**：`docs/testing/`（新增测试方案）
- **验收标准**：
  - 定义评估维度（准确性、完整性、安全性）
  - 定义测试 prompt 集（50 题）
  - 定义评分标准
- **非目标**：不做自动化评分
- **风险**：一致性评估的主观性较高

---

### Issue 28: 端侧模型热更新方案

- **目标**：设计端侧模型文件的热更新方案
- **文件范围**：设计文档，不涉及代码
- **验收标准**：
  - 支持模型文件的增量更新
  - 更新过程中端侧功能降级而非中断
  - 更新失败可回滚
- **非目标**：不做实际的热更新实现
- **风险**：模型文件体积大，增量更新实现复杂

---

### Issue 29: 端侧推理并发控制

- **目标**：设计端侧并发推理的控制策略
- **文件范围**：`core/src/.../provider/`
- **验收标准**：
  - 端侧同时最多一个 generate 请求
  - 并发请求自动排队或降级到云端
  - 排队超时自动降级
- **非目标**：不做端侧并发推理（设备限制）
- **风险**：排队机制可能增加用户感知延迟

---

### Issue 30: Stage 8A-3 材料归档

- **目标**：将 Stage 8A-3 所有材料整理归档，更新 INDEX.md
- **文件范围**：`docs/INDEX.md`, `proof_out/`
- **验收标准**：
  - INDEX.md 更新 Stage 8A-3 小节
  - proof pack 生成并校检
  - 所有新增文档交叉引用正确
- **非目标**：不做代码提交
- **风险**：文档交叉引用可能有遗漏
