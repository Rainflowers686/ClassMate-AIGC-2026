## What & why

<!-- Summary of the change and the learning/product reason for it. -->

## Checklist

- [ ] `:core:test`, `:app:testDebugUnitTest`, `:app:assembleDebug` pass locally.
- [ ] No real AppID / AppKEY / Authorization anywhere (code, tests, docs, logs, screenshots).
- [ ] `config.example.json` still uses only placeholders; no `config.local.json` committed.
- [ ] New model conclusions remain evidence-bound (validator passes).
- [ ] BlueLM remains the primary path; fallback stays a safety net.

## Evidence

<!-- Screenshots / redacted logs. NO secrets. -->
